
import lightThemColors from "./colors/light";

// export const useThemeColor = () => {
//   const scheme = useColorScheme();
//   return scheme === "dark" ? darkThemColors : lightThemColors;
// };

export const Colors = lightThemColors;